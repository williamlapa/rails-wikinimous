<h1>My articles</h1>
<ul>
  <% @articles.each do |article| %>
     <li><%= link_to article.title, article_path(article) %>
     <%= link_to '🚮', article_path(article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
     </li> 
  <% end %>
</ul>
<br>

<div class="container">
  <%= link_to "Add a new article", '/articles/new', class: "btn btn-primary"%>
</div>